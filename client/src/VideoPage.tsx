import React from "react"
import { VideoType } from "./types";

type VideoPageTypes = {
    videoType: VideoType 
}

const VideoPage = ({videoType}: VideoPageTypes) => {
    return( <div></div>);
}

export default VideoPage;